<UserControl x:Class="FrontPage.Pages.ListaConexoes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="Auto" Width="Auto">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Conexões Salvas" FontWeight="Bold" FontSize="16"/>

        <DataGrid x:Name="dgConexoes" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Servidor" Binding="{Binding Servidor}" Width="*"/>
                <DataGridTextColumn Header="Banco" Binding="{Binding Banco}" Width="*"/>
                <DataGridTextColumn Header="Porta" Binding="{Binding Porta}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="Auto"/>
                <DataGridTemplateColumn Header="Ações">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Executar" Click="ExecutarConexao_Click" Margin="2"/>
                                <Button Content="Editar" Click="EditarConexao_Click" Margin="2" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Arquivos .sql" FontWeight="Bold" FontSize="16"/>
            <ListBox x:Name="lbDumps" Height="120" Margin="0,5"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                <Button Content="Carregar Dumps" Click="CarregarDumps_Click" Width="120" Margin="5"/>
                <Button Content="Executar Dump" Click="ExecutarDump_Click" Width="120" Margin="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
